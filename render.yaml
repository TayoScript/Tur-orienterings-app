services:
  - type: web
    name: tur-orienterings-app
    env: static
    buildCommand: cd turorienteringapp/frontend && npm install && npm run build
    staticPublishPath: ./turorienteringapp/frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html 